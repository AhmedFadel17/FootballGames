export enum GameDifficulty {
    EASY = 1,
    NORMAL = 2,
    HARD = 3,
}

export enum GameStatus {
    PENDING = 'pending',
    ACTIVE = 'active',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
}

export enum GameResultStatus {
    PLAYING = 'playing',
    WON = 'won',
    LOST = 'lost',
}
